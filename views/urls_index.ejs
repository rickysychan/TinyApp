<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <% include ./partials/_header %>
    <form method="POST" action="/urls/9sm5xK/delete">...</form>
    <ul>
  <% for(key in urls) { %>
        <li><%= key + ": " + urls[key] %> - </li>
    <% }; %>
    </ul>

<a href="urls/new">shorten a new URL</a>

</body>
</html>